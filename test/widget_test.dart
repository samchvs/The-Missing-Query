// Widget tests are skipped here because MyApp now requires an AuthController
// which itself requires live Supabase and SharedPreferences instances.
// Proper tests should mock AuthController — see test/domain/ for unit tests.

void main() {}
